<view class="container">
  <view class="page-header">
    <text class="header-title">地址管理</text>
  </view>

  <scroll-view class="address-list" scroll-y>
    <view 
      class="address-item"
      a:for="{{addresses}}"
      a:key="id"
      onTap="selectAddress"
      data-id="{{item.id}}"
    >
      <view class="address-card">
        <view class="address-header">
          <view class="address-info">
            <text class="receiver-name">{{item.name}}</text>
            <text class="receiver-phone">{{item.phone}}</text>
          </view>
          <view class="address-actions" catchtap="true">
            <text class="action-btn edit-btn" onTap="editAddress" data-id="{{item.id}}">编辑</text>
            <text class="action-btn delete-btn" onTap="deleteAddress" data-id="{{item.id}}">删除</text>
          </view>
        </view>
        <view class="address-detail-text">
          <text class="location-icon">📍</text>
          <text class="detail-text">{{item.province}}{{item.city}}{{item.district}}{{item.detail}}</text>
        </view>
      </view>
    </view>

    <view class="empty-state" a:if="{{addresses.length === 0}}">
      <text class="empty-icon">📭</text>
      <text class="empty-text">暂无地址</text>
      <text class="empty-desc">点击下方按钮添加您的收货地址</text>
    </view>
  </scroll-view>

  <view class="add-btn-container">
    <button class="add-address-btn" type="primary" onTap="addAddress">添加地址</button>
  </view>
</view>
